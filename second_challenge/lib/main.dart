import 'package:flutter/material.dart';
import 'package:second_challenge/First_Page.dart';
import './orderInfo.dart';

import 'order.dart';


void main() => runApp(MaterialApp(debugShowCheckedModeBanner: false, home: OrderInfo(),));

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(

    );
  }
}