import 'package:flutter/material.dart';
import 'SecondPage.dart';
class SecondPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('For Staffs'),
      ),
      body: Center(
          child: Text('Congra This is Staffs Second Page'),
        ),


    );
  }
}